ngDescribe({
    name: 'Test notification component',
    modules: 'app',
    element: '<notification></notification>',
    tests: function (deps) {

        it('basic test', () => {
            //
        });
    }
});
