ngDescribe({
    name: 'Test institutes component',
    modules: 'app',
    element: '<institutes></institutes>',
    tests: function (deps) {

        it('basic test', () => {
            //
        });
    }
});
