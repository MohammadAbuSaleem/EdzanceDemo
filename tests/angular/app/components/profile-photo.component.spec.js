ngDescribe({
    name: 'Test profile-photo component',
    modules: 'app',
    element: '<profile-photo></profile-photo>',
    tests: function (deps) {

        it('basic test', () => {
            //
        });
    }
});
