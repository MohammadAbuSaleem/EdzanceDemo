ngDescribe({
    name: 'Test virtualclass-unauthorized component',
    modules: 'app',
    element: '<virtualclass-unauthorized></virtualclass-unauthorized>',
    tests: function (deps) {

        it('basic test', () => {
            //
        });
    }
});
