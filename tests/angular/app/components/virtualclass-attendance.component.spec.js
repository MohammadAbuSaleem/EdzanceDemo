ngDescribe({
    name: 'Test virtualclass-attendance component',
    modules: 'app',
    element: '<virtualclass-attendance></virtualclass-attendance>',
    tests: function (deps) {

        it('basic test', () => {
            //
        });
    }
});
