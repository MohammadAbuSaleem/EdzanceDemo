ngDescribe({
    name: 'Test otherwise component',
    modules: 'app',
    element: '<otherwise></otherwise>',
    tests: function (deps) {

        it('basic test', () => {
            //
        });
    }
});
