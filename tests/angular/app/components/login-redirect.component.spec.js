ngDescribe({
    name: 'Test login-redirect component',
    modules: 'app',
    element: '<login-redirect></login-redirect>',
    tests: function (deps) {

        it('basic test', () => {
            //
        });
    }
});
