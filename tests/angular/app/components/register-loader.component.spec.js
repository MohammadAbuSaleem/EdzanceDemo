ngDescribe({
    name: 'Test register-loader component',
    modules: 'app',
    element: '<register-loader></register-loader>',
    tests: function (deps) {

        it('basic test', () => {
            //
        });
    }
});
