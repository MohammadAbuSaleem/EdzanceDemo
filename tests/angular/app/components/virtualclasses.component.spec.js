ngDescribe({
    name: 'Test virtualclasses component',
    modules: 'app',
    element: '<virtualclasses></virtualclasses>',
    tests: function (deps) {

        it('basic test', () => {
            //
        });
    }
});
