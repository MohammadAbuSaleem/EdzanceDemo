ngDescribe({
    name: 'Test message-room component',
    modules: 'app',
    element: '<message-room></message-room>',
    tests: function (deps) {

        it('basic test', () => {
            //
        });
    }
});
