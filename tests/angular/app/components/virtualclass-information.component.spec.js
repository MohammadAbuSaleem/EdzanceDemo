ngDescribe({
    name: 'Test virtualclass-information component',
    modules: 'app',
    element: '<virtualclass-information></virtualclass-information>',
    tests: function (deps) {

        it('basic test', () => {
            //
        });
    }
});
