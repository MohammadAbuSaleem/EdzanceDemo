ngDescribe({
    name: 'Test login-header component',
    modules: 'app',
    element: '<login-header></login-header>',
    tests: function (deps) {

        it('basic test', () => {
            //
        });
    }
});
