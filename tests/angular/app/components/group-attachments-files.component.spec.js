ngDescribe({
    name: 'Test group-attachments-files component',
    modules: 'app',
    element: '<group-attachments-files></group-attachments-files>',
    tests: function (deps) {

        it('basic test', () => {
            //
        });
    }
});
