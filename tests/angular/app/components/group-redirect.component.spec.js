ngDescribe({
    name: 'Test group-redirect component',
    modules: 'app',
    element: '<group-redirect></group-redirect>',
    tests: function (deps) {

        it('basic test', () => {
            //
        });
    }
});
