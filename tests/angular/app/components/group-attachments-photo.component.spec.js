ngDescribe({
    name: 'Test group-attachments-photo component',
    modules: 'app',
    element: '<group-attachments-photo></group-attachments-photo>',
    tests: function (deps) {

        it('basic test', () => {
            //
        });
    }
});
