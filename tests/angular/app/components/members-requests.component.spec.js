ngDescribe({
    name: 'Test members-requests component',
    modules: 'app',
    element: '<members-requests></members-requests>',
    tests: function (deps) {

        it('basic test', () => {
            //
        });
    }
});
