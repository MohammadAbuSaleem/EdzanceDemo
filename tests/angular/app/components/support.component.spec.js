ngDescribe({
    name: 'Test support component',
    modules: 'app',
    element: '<support></support>',
    tests: function (deps) {

        it('basic test', () => {
            //
        });
    }
});
