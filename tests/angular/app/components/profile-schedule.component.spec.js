ngDescribe({
    name: 'Test profile-schedule component',
    modules: 'app',
    element: '<profile-schedule></profile-schedule>',
    tests: function (deps) {

        it('basic test', () => {
            //
        });
    }
});
