ngDescribe({
    name: 'Test register-error component',
    modules: 'app',
    element: '<register-error></register-error>',
    tests: function (deps) {

        it('basic test', () => {
            //
        });
    }
});
