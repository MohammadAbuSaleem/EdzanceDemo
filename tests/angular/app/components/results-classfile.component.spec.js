ngDescribe({
    name: 'Test results-classfile component',
    modules: 'app',
    element: '<results-classfile></results-classfile>',
    tests: function (deps) {

        it('basic test', () => {
            //
        });
    }
});
