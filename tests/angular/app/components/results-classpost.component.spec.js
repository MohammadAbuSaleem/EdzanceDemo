ngDescribe({
    name: 'Test results-classpost component',
    modules: 'app',
    element: '<results-classpost></results-classpost>',
    tests: function (deps) {

        it('basic test', () => {
            //
        });
    }
});
