ngDescribe({
    name: 'Test log-head component',
    modules: 'app',
    element: '<log-head></log-head>',
    tests: function (deps) {

        it('basic test', () => {
            //
        });
    }
});
