ngDescribe({
    name: 'Test register-mail component',
    modules: 'app',
    element: '<register-mail></register-mail>',
    tests: function (deps) {

        it('basic test', () => {
            //
        });
    }
});
