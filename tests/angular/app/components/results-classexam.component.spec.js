ngDescribe({
    name: 'Test results-classexam component',
    modules: 'app',
    element: '<results-classexam></results-classexam>',
    tests: function (deps) {

        it('basic test', () => {
            //
        });
    }
});
